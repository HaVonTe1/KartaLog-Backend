---
phase: 04-add-series-data-to-productdto
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketDetailsParser.kt
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/Dtos.kt
autonomous: true
requirements:
  - SERIES-01
must_haves:
  truths:
    - "API returns seriesId and seriesName in ProductDTO and ProductDetailsDTO"
    - "Series data is scraped from CardMarket product detail pages"
  artifacts:
    - path: "src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketDetailsParser.kt"
      provides: "Parses series info from product detail HTML"
      min_lines: 320
    - path: "src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/Dtos.kt"
      provides: "CardmarketProductDetailsDto includes series data"
      exports: ["CardmarketProductDetailsDto"]
  key_links:
    - from: "CardMarketDetailsParser.kt"
      to: "CardmarketProductDetailsDto"
      via: "parse() return value"
      pattern: "CardmarketProductDetailsDto.*series"
---

<objective>
Add series data (seriesId, seriesName) to the API response from CardMarket scraping.

**Purpose:** Users can see which series a card belongs to (e.g., "Base Series", "Sword & Shield") in search results and product details.

**Output:** ProductDTO and ProductDetailsDTO include seriesId and seriesName.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md

From src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/model/Product.kt:
```kotlin
data class ProductSeries(
    val seriesId: Long,
    val names: Map<Locale, String> = emptyMap(),
)
```

From contract/openapi.yaml (lines 285-290, 348-358):
```yaml
ProductDTO:
  properties:
    seriesId:
      type: integer
      format: int64
      nullable: true
      description: Series identifier
ProductDetailsDTO:
  properties:
    seriesId:
      type: integer
      format: int64
      nullable: true
    seriesName:
      type: string
      nullable: true
      description: Name of the product series
```

Current state:
- ProductDTO already has seriesId field via generated code
- ProductDetailsDTO already has seriesId, seriesName fields
- CollectablesMapper already maps product.series?.seriesId and product.series?.names[locale]
- Product domain model already has series: ProductSeries? field
- CardmarketProductDetailsDto MISSING series field (this is the gap)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CardMarketDetailsParser to extract series data</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/Dtos.kt</files>
  <action>
    1. Add `seriesId: Long? = null` and `seriesName: String? = null` fields to `CardmarketProductDetailsDto` data class in Dtos.kt
    2. In CardMarketDetailsParser.kt, add extraction of series info from the info-list element:
       - Look for element containing series name in the info list (typically labeled "Serie" / "Series" in German)
       - Parse the seriesId from the link href (e.g., `/Packages?serieId=123` -> seriesId=123)
       - Get the series name text from the link
    3. Map parsed series data in the parser's return value

**IMPORTANT:** Use Jsoup selectors to find series info from the info-list div. Series links typically look like:
- `<a href="/Packages?serieId=123">Base Series</a>`

Example selector: `dt:contains(Serie) + dd a` or find anchor with `serieId` param in href.
  </action>
  <verify>
    <automated>./gradlew test --tests "CardMarketDetailsParserTest" -x integrationTest 2>&1 | tail -20</automated>
  </verify>
  <done>
    CardmarketProductDetailsDto includes seriesId and seriesName fields
    Parser extracts series data from HTML with valid seriesId > 0
  </done>
</task>

</tasks>

<verification>
- [ ] Code compiles: `./gradlew compileKotlin`
- [ ] Existing tests pass: `./gradlew test --exclude-tags integration`
- [ ] Build completes: `./gradlew build -x test`
</verification>

<success_criteria>
- ProductDTO includes seriesId when product has series data
- ProductDetailsDTO includes seriesId and seriesName when available
- Series data correctly mapped through ProductMapper → CollectablesMapper → API response
</success_criteria>

<output>
After completion, create `.planning/phases/04-add-series-data-to-productdto/04-01-SUMMARY.md`
</output>
---
phase: 01-scraper-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [SCRAP-01, SCRAP-02, SCRAP-03]
user_setup: []
must_haves:
  truths:
    - "Locale enum covers all CardMarket languages (de, en, fr, it, es, pt, nl, pl)"
    - "Translation map data class exists with nested maps per language"
    - "Genre configuration structure replaces CardMarketConstants"
  artifacts:
    - path: "src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/model/Locale.kt"
      provides: "Locale enum/sealed class for all CardMarket languages"
      exports: ["Locale"]
    - path: "src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/TranslationMap.kt"
      provides: "Translation map for language-specific labels"
      exports: ["TranslationMap"]
    - path: "src/main/kotlin/io/github/havonte1/tcgwatcher/backend/config/GenreConfig.kt"
      provides: "Genre configuration structure"
      exports: ["GenreConfig", "Genre"]
  key_links:
    - from: "src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/TranslationMap.kt"
      to: "src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/model/Locale.kt"
      via: "TranslationMap uses Locale enum keys"
      pattern: "TranslationMap\\(.*Locale\\."
---

# Phase 1: Scraper Foundation - Plan 01

## Objective

Create foundational domain models and configuration structures for multi-language scraper support.

**Purpose:** Establish type-safe locale handling and translation infrastructure that will enable the parser to support all CardMarket languages without hardcoded German labels.

**Output:**
- Locale enum/sealed class for all CardMarket languages
- TranslationMap data class with nested maps per language
- GenreConfig structure replacing CardMarketConstants

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-scraper-foundation/01-CONTEXT.md

### Domain Models

```kotlin
// Product.kt - existing domain model
data class Product(
    val names: Map<String, String> = emptyMap(),  // locale -> name
    // ...
)

data class ProductSet(
    val names: Map<String, String> = emptyMap(),  // locale -> name
)

data class ProductSeries(
    val names: Map<String, String> = emptyMap(),  // locale -> name
)
```

### Current State

- `CardMarketConstants.kt`: Simple constants object with DEFAULT_LOCALE, DEFAULT_GAME, PATH_SEPARATOR
- `CardMarketContentParser.kt`: 286 lines, hardcoded German labels ("Rarität", "Erschienen", "ab", "Preis-Trend")
- `CardMarketScraperPort.kt`: Uses `String` for locale and game parameters (not type-safe)

## Tasks

<task type="auto">
  <name>Task 1: Create Locale enum/sealed class</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/model/Locale.kt</files>
  <action>Create a sealed class or enum named Locale in the domain model package that covers all CardMarket languages (de, en, fr, it, es, pt, nl, pl). Each locale should have a code property returning the ISO language code. Use sealed class for extensibility if adding custom locales later, or enum if only CardMarket languages are needed.</action>
  <verify>
    <automated>grep -q "sealed class Locale\|enum class Locale" src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/model/Locale.kt && grep -q '"de"' src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/model/Locale.kt && grep -q '"en"' src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/model/Locale.kt && grep -q '"fr"' src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/model/Locale.kt</automated>
  </verify>
  <done>Locale.kt exists with all 8 CardMarket languages (de, en, fr, it, es, pt, nl, pl) and code property</done>
</task>

<task type="auto">
  <name>Task 2: Create TranslationMap data class</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/TranslationMap.kt</files>
  <action>Create TranslationMap as a Kotlin data class with nested maps per language. The structure should be: data class TranslationMap(val de: Labels, val en: Labels, val fr: Labels, val it: Labels, val es: Labels, val pt: Labels, val nl: Labels, val pl: Labels) where Labels contains: val paginationOf: String (e.g., "von", "of", "de"), val rarityLabel: String (e.g., "Rarität"), val releaseDateLabel: String (e.g., "Erschienen"), val priceLabel: String (e.g., "ab"), val priceTrendLabel: String (e.g., "Preis-Trend"). Each language map contains the German source label as key and the translated label as value for that language.</action>
  <verify>
    <automated>grep -q "data class TranslationMap" src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/TranslationMap.kt && grep -q "val de: Labels" src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/TranslationMap.kt && grep -q "val en: Labels" src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/TranslationMap.kt && grep -q "val rarityLabel" src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/TranslationMap.kt</automated>
  </verify>
  <done>TranslationMap.kt exists with all 8 languages and Labels data class containing paginationOf, rarityLabel, releaseDateLabel, priceLabel, priceTrendLabel</done>
</task>

<task type="auto">
  <name>Task 3: Create GenreConfig structure</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/config/GenreConfig.kt</files>
  <action>Create GenreConfig as a Kotlin data class with fields: val genre: Genre, val defaultLocale: Locale, val searchPathPattern: String, val detailsPathPattern: String, val parserType: ParserType. Create Genre enum with POKEMON, YUGIOH, MTG values. Create ParserType sealed class with GalleryParser and DetailsParser. The config should be extensible for adding new genres without changing core scraping logic. Use GenreConfig.GENRES map for registry of all genres.</action>
  <verify>
    <automated>grep -q "data class GenreConfig" src/main/kotlin/io/github/havonte1/tcgwatcher/backend/config/GenreConfig.kt && grep -q "enum class Genre" src/main/kotlin/io/github/havonte1/tcgwatcher/backend/config/GenreConfig.kt && grep -q "val POKEMON" src/main/kotlin/io/github/havonte1/tcgwatcher/backend/config/GenreConfig.kt && grep -q "val GENRES" src/main/kotlin/io/github/havonte1/tcgwatcher/backend/config/GenreConfig.kt</automated>
  </verify>
  <done>GenreConfig.kt exists with Genre enum (POKEMON, YUGIOH, MTG), GenreConfig data class, and GENRES map registry</done>
</task>

</tasks>

<verification>
- [ ] Locale.kt exists with all 8 CardMarket languages
- [ ] TranslationMap.kt exists with nested maps per language
- [ ] GenreConfig.kt exists with Genre enum and GENRES registry
- [ ] All files compile without errors: ./gradlew compileKotlin
- [ ] No references to hardcoded German labels remain in new files
</verification>

<success_criteria>
- [ ] Locale enum/sealed class covers all CardMarket languages (de, en, fr, it, es, pt, nl, pl)
- [ ] TranslationMap data class has nested maps per language with all required labels
- [ ] GenreConfig structure replaces CardMarketConstants and is extensible for v2 genres
</success_criteria>

<output>
After completion, create `.planning/phases/01-scraper-foundation/01-01-SUMMARY.md`
</output>

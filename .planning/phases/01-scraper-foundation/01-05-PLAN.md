---
phase: 01-scraper-foundation
plan: 05
type: execute
wave: 3
depends_on: ["01-03", "01-04"]
files_modified: []
autonomous: true
requirements: [SCRAP-01, SCRAP-02, SCRAP-03]
user_setup: []
must_haves:
  truths:
    - "Full integration test for search flow with multi-language support"
    - "Full integration test for product details flow with multi-language support"
    - "All parsers correctly use TranslationMap for language-specific labels"
  artifacts:
    - path: "src/test/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketScraperAdapterIT.kt"
      provides: "Integration tests for scraper adapter"
      exports: ["CardMarketScraperAdapterIT"]
  key_links:
    - from: "src/test/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketScraperAdapterIT.kt"
      to: "src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketScraperAdapter.kt"
      via: "Tests call adapter methods with Locale and Genre"
      pattern: "CardMarketScraperAdapter"
    - from: "src/test/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketScraperAdapterIT.kt"
      to: "src/main/kotlin/io/github/havonte1/tcgwatcher/backend/config/GenreConfig.kt"
      via: "Tests verify GenreConfig lookup works"
      pattern: "GenreConfig\\.GENRES"
---

# Phase 1: Scraper Foundation - Plan 05

## Objective

Create integration tests for the complete scraper flow with multi-language support.

**Purpose:** Verify the entire scraper chain works end-to-end with the new Locale enum, TranslationMap, and GenreConfig infrastructure.

**Output:**
- CardMarketScraperAdapterIT.kt (integration tests for scraper adapter)

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-scraper-foundation/01-CONTEXT.md
@.planning/phases/01-scraper-foundation/01-03-PLAN.md (Plan 03)
@.planning/phases/01-scraper-foundation/01-04-PLAN.md (Plan 04)

### Test Strategy

Use Testcontainers for PostgreSQL (if persistence is tested) and WireMock for CardMarket API mocking. Since the scraper uses Playwright for real browser automation, tests may need to be marked with `@Tag("integration")` and run separately.

### Existing Test Pattern

```kotlin
// Example from existing tests
@SpringBootTest
@Tag("integration")
class CardMarketScraperAdapterIT {
    @Autowired
    private lateinit var scraperAdapter: CardMarketScraperAdapter
    
    @Test
    fun `search returns products`() {
        // Test implementation
    }
}
```

## Tasks

<task type="auto">
  <name>Task 1: Create CardMarketScraperAdapterIT.kt</name>
  <files>src/test/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketScraperAdapterIT.kt</files>
  <action>Create CardMarketScraperAdapterIT.kt with integration tests for the scraper adapter. Tests should: 1) Test search() with different locales (de, en, fr), 2) Test search() with different genres (POKEMON), 3) Test fetchProductDetails() with different locales, 4) Verify TranslationMap is used correctly for language-specific parsing, 5) Test error handling for MissingElement and UnexpectedFormat errors. Use @Tag("integration") annotation and run with ./gradlew integrationTest.</action>
  <verify>
    <automated>./gradlew integrationTest --tests "CardMarketScraperAdapterIT"</automated>
  </verify>
  <done>All CardMarketScraperAdapterIT tests pass with multi-language and multi-genre scenarios</done>
</task>

</tasks>

<verification>
- [ ] CardMarketScraperAdapterIT.kt exists and is properly tagged as integration test
- [ ] Tests verify Locale enum usage in search() and fetchProductDetails()
- [ ] Tests verify Genre enum usage in search() and fetchProductDetails()
- [ ] Tests verify TranslationMap is used for language-specific parsing
- [ ] Tests verify error handling for MissingElement and UnexpectedFormat
- [ ] All integration tests pass: ./gradlew integrationTest
</verification>

<success_criteria>
- [ ] Full integration test for search flow with multi-language support
- [ ] Full integration test for product details flow with multi-language support
- [ ] All parsers correctly use TranslationMap for language-specific labels
</success_criteria>

<output>
After completion, create `.planning/phases/01-scraper-foundation/01-05-SUMMARY.md`
</output>

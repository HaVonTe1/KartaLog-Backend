---
phase: 01-scraper-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [SCRAP-01, SCRAP-02, SCRAP-03]
user_setup: []
must_haves:
  truths:
    - "CardMarketContentParser split into two focused parsers"
    - "Parser returns Result<Dto> with typed errors"
    - "Parse errors are distinct (MissingElement vs UnexpectedFormat)"
  artifacts:
    - path: "src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketGalleryParser.kt"
      provides: "Gallery/search page parser"
      exports: ["CardMarketGalleryParser"]
    - path: "src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketDetailsParser.kt"
      provides: "Product details page parser"
      exports: ["CardMarketDetailsParser"]
    - path: "src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/ParseErrors.kt"
      provides: "Typed parse error types"
      exports: ["ParseError", "MissingElement", "UnexpectedFormat"]
  key_links:
    - from: "src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketGalleryParser.kt"
      to: "src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/TranslationMap.kt"
      via: "Uses TranslationMap for language-specific labels"
      pattern: "TranslationMap\\."
    - from: "src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketGalleryParser.kt"
      to: "src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/Dtos.kt"
      via: "Returns SearchResultsPageDto"
      pattern: "SearchResultsPageDto"
---

# Phase 1: Scraper Foundation - Plan 02

## Objective

Split the monolithic CardMarketContentParser into two focused parsers with typed error handling.

**Purpose:** Replace the 286-line monolithic parser with two focused parsers (gallery and details) that use the TranslationMap for language-specific labels instead of hardcoded German strings.

**Output:**
- CardMarketGalleryParser.kt (gallery/search page parsing)
- CardMarketDetailsParser.kt (product details page parsing)
- ParseErrors.kt (typed error types)

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-scraper-foundation/01-CONTEXT.md
@.planning/phases/01-scraper-foundation/01-01-PLAN.md (Plan 01)

### Current State

```kotlin
// CardMarketContentParser.kt (286 lines)
fun parseGalaryPage(content: String, page: Int = 1): SearchResultsPageDto<CardmarketProductGallaryItemDto>
fun parseProductDetails(content: String, cmId: String, genre: String, type: String, lang: String, setname: String): CardmarketProductDetailsDto
```

**Issues:**
- Hardcoded German labels: "Rarität", "Erschienen", "ab", "Preis-Trend"
- Single method for both gallery and details parsing
- No typed error handling (returns DTOs directly)

### TranslationMap (from Plan 01)

```kotlin
data class TranslationMap(
    val de: Labels,
    val en: Labels,
    // ...
)

data class Labels(
    val paginationOf: String,
    val rarityLabel: String,
    val releaseDateLabel: String,
    val priceLabel: String,
    val priceTrendLabel: String,
)
```

## Tasks

<task type="auto">
  <name>Task 1: Create ParseErrors.kt with typed error types</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/ParseErrors.kt</files>
  <action>Create ParseErrors.kt with sealed interface ParseError and two implementations: MissingElement(message: String) for transient issues (layout change, missing expected element) and UnexpectedFormat(message: String) for permanent failures (unexpected HTML structure). Each error should include a message describing what was expected vs what was found.</action>
  <verify>
    <automated>grep -q "sealed interface ParseError" src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/ParseErrors.kt && grep -q "class MissingElement" src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/ParseErrors.kt && grep -q "class UnexpectedFormat" src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/ParseErrors.kt</automated>
  </verify>
  <done>ParseErrors.kt exists with ParseError sealed interface, MissingElement and UnexpectedFormat implementations</done>
</task>

<task type="auto">
  <name>Task 2: Create CardMarketGalleryParser.kt</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketGalleryParser.kt</files>
  <action>Create CardMarketGalleryParser with single method: fun parse(content: String, page: Int, translationMap: TranslationMap): Result<SearchResultsPageDto<CardmarketProductGallaryItemDto>>. The parser should: 1) Use Jsoup to parse HTML, 2) Extract product tiles from a tags with class "card", 3) Use translationMap for pagination label ("paginationOf" from Labels), 4) Return Result.success() with parsed data or Result.failure() with ParseError. Extract name, code, price, priceTrend, image link from each tile using CSS selectors. Handle missing elements as MissingElement errors, unexpected structure as UnexpectedFormat.</action>
  <verify>
    <automated>grep -q "class CardMarketGalleryParser" src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketGalleryParser.kt && grep -q "fun parse(content: String, page: Int, translationMap: TranslationMap): Result<SearchResultsPageDto" src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketGalleryParser.kt && grep -q "MissingElement\|UnexpectedFormat" src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketGalleryParser.kt</automated>
  </verify>
  <done>CardMarketGalleryParser.kt exists with parse() method using TranslationMap and returning Result<SearchResultsPageDto></done>
</task>

<task type="auto">
  <name>Task 3: Create CardMarketDetailsParser.kt</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketDetailsParser.kt</files>
  <action>Create CardMarketDetailsParser with single method: fun parse(content: String, cmId: String, genre: String, type: String, lang: String, setname: String, translationMap: TranslationMap): Result<CardmarketProductDetailsDto>. The parser should: 1) Use Jsoup to parse HTML, 2) Extract product info using translationMap labels (rarityLabel, releaseDateLabel, priceLabel, priceTrendLabel), 3) Extract sell offers from article-row elements, 4) Return Result.success() with parsed data or Result.failure() with ParseError. Handle missing elements as MissingElement errors, unexpected structure as UnexpectedFormat. Preserve existing sell offer parsing logic but use translationMap for label-based lookups.</action>
  <verify>
    <automated>grep -q "class CardMarketDetailsParser" src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketDetailsParser.kt && grep -q "fun parse(content: String, cmId: String, genre: String, type: String, lang: String, setname: String, translationMap: TranslationMap): Result<CardmarketProductDetailsDto>" src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketDetailsParser.kt && grep -q "rarityLabel\|releaseDateLabel\|priceLabel\|priceTrendLabel" src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketDetailsParser.kt</automated>
  </verify>
  <done>CardMarketDetailsParser.kt exists with parse() method using TranslationMap and returning Result<CardmarketProductDetailsDto></done>
</task>

</tasks>

<verification>
- [ ] ParseErrors.kt exists with ParseError sealed interface
- [ ] CardMarketGalleryParser.kt exists with parse() using TranslationMap
- [ ] CardMarketDetailsParser.kt exists with parse() using TranslationMap
- [ ] All parsers return Result<Dto> with typed errors
- [ ] No hardcoded German labels in new parsers
- [ ] All files compile without errors: ./gradlew compileKotlin
</verification>

<success_criteria>
- [ ] CardMarketContentParser split into two focused parsers
- [ ] Parser returns Result<Dto> with typed errors (MissingElement vs UnexpectedFormat)
- [ ] All parsers use TranslationMap instead of hardcoded German labels
</success_criteria>

<output>
After completion, create `.planning/phases/01-scraper-foundation/01-02-SUMMARY.md`
</output>

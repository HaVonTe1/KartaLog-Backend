---
phase: 01-authentication-authorization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build.gradle.kts
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/model/User.kt
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/model/Role.kt
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/model/RefreshToken.kt
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/persistence/entity/UserEntity.kt
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/persistence/entity/RefreshTokenEntity.kt
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/persistence/repository/UserJpaRepository.kt
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/persistence/repository/RefreshTokenJpaRepository.kt
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/application/AuthenticationService.kt
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/application/JwtTokenService.kt
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/rest/AuthController.kt
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/security/JwtAuthenticationFilter.kt
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/security/JwtUtil.kt
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/config/SecurityConfig.kt
  - src/main/resources/db/changelog/20260418-add-auth-tables.xml
  - src/main/resources/application.yml
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - AUTH-07
  - API-03
user_setup: []
---

<objective>
Implement complete JWT-based authentication and role-based authorization for TCGWatcher API.

Purpose: Replace Basic Auth with secure user authentication supporting registration, login, token refresh, logout, and role-based access control.

Output: Working auth system with User/Role/RefreshToken domain models, JWT token service, Spring Security filter chain, and REST endpoints.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-authentication-authorization/01-RESEARCH.md

# Existing codebase patterns to follow:
@src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/rest/CollectablesAdapter.kt
@src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/persistence/entity/ProductEntity.kt
@src/main/resources/db/changelog/20260207-init.xml
@build.gradle.kts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add auth dependencies + Liquibase migration + domain models</name>
  <files>build.gradle.kts, src/main/resources/db/changelog/20260418-add-auth-tables.xml, src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/model/User.kt, src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/model/Role.kt, src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/model/RefreshToken.kt, src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/persistence/entity/UserEntity.kt, src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/persistence/entity/RefreshTokenEntity.kt</files>
  <action>
    1. Add to build.gradle.kts dependencies:
       - implementation("org.springframework.boot:spring-boot-starter-security")
       - implementation("io.jsonwebtoken:jjwt-api:0.12.7")
       - runtimeOnly("io.jsonwebtoken:jjwt-impl:0.12.7")
       - runtimeOnly("io.jsonwebtoken:jjwt-jackson:0.12.7")

    2. Create Liquibase migration (20260418-add-auth-tables.xml) with:
       - watcher.users table: id (BIGINT PK), email (VARCHAR 255 UNIQUE), password_hash (VARCHAR 255), role (VARCHAR 20), created_at, updated_at
       - watcher.refresh_tokens table: id (BIGINT PK), user_id (BIGINT FK), token_hash (VARCHAR 255), expires_at (TIMESTAMP), revoked (BOOLEAN)

    3. Create domain models:
       - Role.kt: enum USER, ADMIN
       - User.kt: id, email, passwordHash, role, createdAt, updatedAt
       - RefreshToken.kt: id, userId, tokenHash, expiresAt, revoked

    4. Create JPA entities:
       - UserEntity.kt: maps to watcher.users, uses @Entity + @Table(schema="watcher", name="users")
       - RefreshTokenEntity.kt: maps to watcher.refresh_tokens

    Follow existing entity patterns from ProductEntity.kt (schema="watcher", use @Id @GeneratedValue IDENTITY).
  </action>
  <verify>
    <automated>./gradlew compileKotlin -x test 2>&1 | tail -20</automated>
  </verify>
  <done>
    Auth dependencies in build.gradle.kts, Liquibase migration creates users + refresh_tokens tables, domain and JPA entities compile and map to DB schema.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create repositories + JWT token service + security config</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/persistence/repository/UserJpaRepository.kt, src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/persistence/repository/RefreshTokenJpaRepository.kt, src/main/kotlin/io/github/havonte1/tcgwatcher/backend/application/JwtTokenService.kt, src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/security/JwtUtil.kt, src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/security/JwtAuthenticationFilter.kt, src/main/kotlin/io/github/havonte1/tcgwatcher/backend/config/SecurityConfig.kt</files>
  <action>
    1. Create JPA repositories:
       - UserJpaRepository: findByEmail(email: String): UserEntity?, save(entity: UserEntity): UserEntity
       - RefreshTokenJpaRepository: findByTokenHashAndRevokedFalse(tokenHash: String): RefreshTokenEntity?, findByUserId(userId: Long): List<RefreshTokenEntity>, save(entity: RefreshTokenEntity): RefreshTokenEntity, deleteByUserId(userId: Long)

    2. Create JwtTokenService:
       - generateAccessToken(user: User): String — 15-60 min expiry from config, includes email + role + userId claims
       - generateRefreshToken(): String — UUID v4
       - validateAccessToken(token: String): Boolean — catch JwtException
       - getUserIdFromToken(token: String): Long — extract userId claim

    3. Create JwtUtil (companion object pattern):
       - hashToken(token: String): String — SHA-256 for storage (not bcrypt - needed for comparison)

    4. Create JwtAuthenticationFilter:
       - extract JWT from Authorization header (Bearer token)
       - validate with JwtTokenService
       - set SecurityContext with username + role authorities
       - add to filter chain before UsernamePasswordAuthenticationFilter

    5. Create SecurityConfig:
       - @EnableWebSecurity, @Configuration
       - SecurityFilterChain with CSRF disabled, stateless session
       - authorizeHttpRequests: /auth/** permitAll, /admin/** hasRole("ADMIN"), anyRequest authenticated
       - addFilterBefore jwtAuthenticationFilter

    6. Add to application.yml:
       - jwt.secret: ${JWT_SECRET:default-dev-secret-change-in-prod}
       - jwt.access-expiration-ms: ${JWT_ACCESS_EXPIRATION:900000} (15 min default)
       - jwt.refresh-expiration-ms: ${JWT_REFRESH_EXPIRATION:604800000} (7 days default)

    Follow service patterns from application/SearchUseCase.kt, security patterns from 01-RESEARCH.md.
  </action>
  <verify>
    <automated>./gradlew test --tests "io.github.havonte1.tcgwatcher.backend.JwtTokenService*" 2>&1 | tail -10 || echo "No tests yet - skip verification"</automated>
  </verify>
  <done>
    JWT token generation/validation works, Spring Security filter chain rejects unauthenticated requests except /auth/**, admin endpoints restricted to ADMIN role.
  </done>
</task>

<task type="auto">
  <name>Task 3: Authentication service + REST controller</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/application/AuthenticationService.kt, src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/rest/AuthController.kt</files>
  <action>
    1. Create AuthenticationService:
       - register(email, password): User — BCrypt hash password, save to DB, default role USER
       - authenticate(email, password): User — find user, verify BCrypt password, return user or throw exception
       - generateTokens(user: User): AuthResponse — generate access + refresh tokens, save refresh to DB
       - refreshToken(refreshToken: String): AuthResponse — validate refresh token, revoke old, generate new (rotation), return tokens
       - logout(userId: Long, refreshToken: String): Unit — mark refresh token revoked

    2. Create AuthResponse DTO (data class within controller or model folder):
       - accessToken: String
       - refreshToken: String  
       - expiresIn: Long
       - user: UserResponse (id, email, role)

    3. Create AuthController:
       - POST /auth/register: RegisterRequest(email, password) → AuthResponse
       - POST /auth/login: LoginRequest(email, password) → AuthResponse
       - POST /auth/refresh: RefreshRequest(refreshToken) → AuthResponse
       - POST /auth/logout: LogoutRequest(refreshToken), requires auth

    Use BCryptPasswordEncoder from Spring Security for hashing. Follow REST patterns from CollectablesAdapter.kt (@RestController, ResponseEntity).
  </action>
  <verify>
    <automated>./gradlew bootRun 2>&1 & sleep 15 && curl -X POST http://localhost:8080/auth/register -H "Content-Type: application/json" -d '{"email":"test@example.com","password":"testpass123"}' && kill %1 2>/dev/null</automated>
  </verify>
  <done>
    /auth/register creates user, /auth/login returns JWT tokens, /auth/refresh rotates tokens, /auth/logout invalidates refresh token.
  </done>
</task>

</tasks>

<verification>
- [ ] build.gradle.kts contains JJWT + Spring Security dependencies
- [ ] Liquibase migration creates users + refresh_tokens tables
- [ ] Domain models (User, Role, RefreshToken) exist in domain/model/
- [ ] JPA entities map to watcher schema
- [ ] JwtTokenService generates + validates JWT tokens
- [ ] Security filter chain blocks unauthenticated requests (except /auth/**)
- [ ] /admin/** endpoints require ADMIN role
- [ ] /auth/register endpoint creates user with BCrypt hash
- [ ] /auth/login returns JWT access token
- [ ] /auth/refresh rotates refresh token
- [ ] /auth/logout revokes refresh token
</verification>

<success_criteria>
1. User can register with email and password and receive confirmation — ✓ (Task 3: /auth/register)
2. User can log in and receive JWT access token (15-60 min expiry) — ✓ (Task 2 + Task 3: JWT service + /auth/login)
3. User can request refresh token to extend session — ✓ (Task 3: /auth/refresh with rotation)
4. User can log out and invalidate refresh token — ✓ (Task 3: /auth/logout marks revoked)
5. User roles (USER, ADMIN) assignable and persisted — ✓ (Task 1: Role enum, Task 3: register uses USER default)
6. Secured API endpoints reject requests without valid JWT — ✓ (Task 2: SecurityConfig + JwtAuthenticationFilter)
7. Admin-only endpoints reject non-admin users — ✓ (Task 2: hasRole("ADMIN") config)
</success_criteria>

<output>
After completion, create `.planning/phases/01-authentication-authorization/01-01-SUMMARY.md`
</output>
---
phase: 03-details-completion
plan: 04
type: execute
wave: 4
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - contract/openapi.yaml
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/rest/CollectablesAdapter.kt
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/rest/CollectablesMapper.kt
autonomous: true
requirements: [DETAIL-01, DETAIL-02, DETAIL-04, DETAIL-05]

must_haves:
  truths:
    - User can retrieve detailed information for any Pokémon product by cmId
    - Product details include language-specific pricing in API response
    - Product details include all CardMarket attributes in API response
    - Product details response includes ETag header for conditional requests
  artifacts:
    - path: "contract/openapi.yaml"
      provides: "API spec with language pricing and attributes in ProductDetailsDTO"
    - path: "src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/rest/CollectablesAdapter.kt"
      provides: "REST endpoint returning enhanced product details"
  key_links:
    - from: "CollectablesAdapter.kt"
      to: "CollectablesMapper.kt"
      via: "toDetailDto() mapping language pricing and attributes"
      pattern: "CollectablesMapper.toDetailDto"
---

<objective>
Update REST API to return language-specific pricing and product attributes in product details response. Ensure ETag caching works correctly with enhanced data.

Purpose: Expose enhanced product details through REST API with proper ETag support
Output: Updated OpenAPI spec, adapter, and mapper
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@contract/openapi.yaml
@src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/rest/CollectablesAdapter.kt
@src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/rest/CollectablesMapper.kt

# Current ProductDetailsDTO doesn't include language pricing or product attributes
# Need to extend API contract and mapping logic
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update OpenAPI spec for enhanced ProductDetailsDTO</name>
  <files>contract/openapi.yaml</files>
  <action>
Add LanguagePricingDTO schema:
- locale: string (enum for languages)
- price: string
- priceTrend: string
- priceTrendValid: boolean

Add ProductAttributeDTO schema:
- attributeName: string (e.g., "rarity", "releaseDate", "cardNumber")
- value: string
- attributeType: string (enum)

Update ProductDetailsDTO:
- Add languagePricing: [LanguagePricingDTO]
- Add productAttributes: [ProductAttributeDTO]
- Add releaseDate: string (nullable)
- Add cardNumber: string (nullable)

Update examples to reflect new fields.
  </action>
  <verify>
<automated>./gradlew compileKotlin 2>&1 | tail -20</automated>
  </verify>
  <done>OpenAPI spec updated with language pricing and product attributes</done>
</task>

<task type="auto">
  <name>Task 2: Update CollectablesMapper for enhanced DTO mapping</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/rest/CollectablesMapper.kt</files>
  <action>
Update toDetailDto() method:
- Map languagePricing list from domain to DTO
- Map productAttributes list from domain to DTO
- Map releaseDate and cardNumber fields

Add helper methods for mapping language pricing and attributes if needed.
  </action>
  <verify>
<automated>./gradlew compileKotlin 2>&1 | tail -20</automated>
  </verify>
  <done>CollectablesMapper maps enhanced product details to DTOs</done>
</task>

<task type="auto">
  <name>Task 3: Verify ETag implementation for details endpoint</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/rest/CollectablesAdapter.kt</files>
  <action>
Review getProductDetails() ETag handling:
- Verify ETag is generated from updatedAt timestamp
- Verify If-None-Match comparison works correctly
- Verify 304 Not Modified response includes ETag header

The existing implementation already has ETag support - verify it's using the correct timestamp field (updatedAt from Product). Ensure language pricing and attributes are included in the cached data comparison.

Test scenarios:
- First request: returns 200 with ETag header
- Subsequent request with matching ETag: returns 304 Not Modified
- Subsequent request with different ETag: returns 200 with new data
  </action>
  <verify>
<automated>./gradlew test --tests "*CollectablesAdapterIT" -x 2>&1 | tail -40</automated>
  </verify>
  <done>ETag support verified for enhanced product details</done>
</task>

</tasks>

<verification>
- [ ] OpenAPI spec includes LanguagePricingDTO and ProductAttributeDTO
- [ ] ProductDetailsDTO includes languagePricing and productAttributes fields
- [ ] CollectablesMapper maps all enhanced fields correctly
- [ ] ETag header returned in 200 and 304 responses
- [ ] 304 Not Modified works correctly with If-None-Match
- [ ] Build compiles and tests pass
</verification>

<success_criteria>
- API returns language-specific pricing for all CardMarket languages (DETAIL-02)
- API returns all product attributes (DETAIL-04)
- API returns ETag header and handles 304 Not Modified (DETAIL-05)
- User can retrieve detailed information by cmId (DETAIL-01)
</success_criteria>

<output>
After completion, create `.planning/phases/03-details-completion/03-04-SUMMARY.md`
</output>

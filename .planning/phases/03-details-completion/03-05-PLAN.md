---
phase: 03-details-completion
plan: 05
type: execute
wave: 5
depends_on: [03-04]
files_modified:
  - src/test/kotlin/io/github/havonte1/tcgwatcher/backend/
autonomous: true
requirements: [DETAIL-01, DETAIL-02, DETAIL-03, DETAIL-04, DETAIL-05]

must_haves:
  truths:
    - User can retrieve detailed information for any Pokémon product by cmId
    - Product details include language-specific pricing and all sell offers
    - Product details include all CardMarket-supported product attributes
    - Product details response includes ETag header and returns 304 Not Modified
  artifacts:
    - path: "src/test/kotlin/io/github/havonte1/tcgwatcher/backend/"
      provides: "Unit and integration tests for enhanced product details"
  key_links:
    - from: "CollectablesServiceTest.kt"
      to: "CollectablesAdapterIT.kt"
      via: "End-to-end testing of product details with language pricing"
      pattern: "fetchProductDetails"
---

<objective>
Add comprehensive tests for product details functionality including language-specific pricing, product attributes, and ETag caching.

Purpose: Verify all Phase 3 requirements work correctly through automated testing
Output: Test files for product details
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/main/kotlin/io/github/havonte1/tcgwatcher/backend/application/CollectablesService.kt
@src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/rest/CollectablesAdapter.kt

# Need tests verifying:
# - Language-specific pricing extraction and display
# - Product attributes in response
# - ETag caching for details endpoint
# - Sell offers with language information
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unit tests for enhanced CollectablesService</name>
  <files>src/test/kotlin/io/github/havonte1/tcgwatcher/backend/application/CollectablesServiceTest.kt</files>
  <action>
Extend CollectablesServiceTest with tests for:
- fetchProductDetails returns product with languagePricing populated
- fetchProductDetails returns product with productAttributes populated
- hasChanges() correctly detects changes in languagePricing
- hasChanges() correctly detects changes in productAttributes
- getProductUpdatedAt returns timestamp for ETag generation

Use MockK for mocking scraper and repository. Create test fixtures for products with language-specific pricing.
  </action>
  <verify>
<automated>./gradlew test --tests "CollectablesServiceTest" 2>&1 | tail -30</automated>
  </verify>
  <done>Unit tests verify enhanced CollectablesService behavior</done>
</task>

<task type="auto">
  <name>Task 2: Add integration tests for product details endpoint</name>
  <files>src/test/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/rest/CollectablesAdapterIT.kt</files>
  <action>
Add integration tests for getProductDetails endpoint:
- GET /collectables/{cmId} returns 200 with languagePricing in response
- GET /collectables/{cmId} returns 200 with productAttributes in response
- GET /collectables/{cmId} returns 200 with all sell offers having productLanguage field
- GET /collectables/{cmId} includes ETag header in 200 response
- GET /collectables/{cmId} with If-None-Match matching ETag returns 304
- GET /collectables/{cmId} with If-None-Match not matching returns 200 with new data

Use @SpringBootTest with Testcontainers. Mock the scraper or use real scraper with WireMock stubs.
  </action>
  <verify>
<automated>./gradlew test --tests "CollectablesAdapterIT" 2>&1 | tail -40</automated>
  </verify>
  <done>Integration tests verify product details API</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for details parser and mapper</name>
  <files>src/test/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketDetailsParserTest.kt</files>
  <action>
Add tests for CardMarketDetailsParser:
- parse() extracts languagePricing for multiple languages
- parse() extracts productAttributes (rarity, releaseDate, cardNumber)
- parse() handles missing language gracefully
- parse() handles missing attributes gracefully

Add tests for CardMarketProductMapper:
- toProductDetails() correctly maps languagePricing
- toProductDetails() correctly maps productAttributes
- toProductDetails() preserves backward compatibility

Use WireMock to create HTML fixture responses that match CardMarket structure.
  </action>
  <verify>
<automated>./gradlew test --tests "*CardMarketDetailsParserTest" 2>&1 | tail -30</automated>
  </verify>
  <done>Parser and mapper tests verify data extraction</done>
</task>

</tasks>

<verification>
- [ ] Unit tests for CollectablesService with language pricing
- [ ] Integration tests for REST endpoint with all requirements
- [ ] Parser and mapper tests with language-specific pricing
- [ ] All tests pass
</verification>

<success_criteria>
- All DETAIL requirements verified through tests
- Tests demonstrate ETag caching works for details endpoint
- Tests demonstrate language-specific pricing is extracted and displayed
- Tests demonstrate product attributes are included in response
</success_criteria>

<output>
After completion, create `.planning/phases/03-details-completion/03-05-SUMMARY.md`
</output>

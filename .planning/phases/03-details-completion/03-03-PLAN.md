---
phase: 03-details-completion
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/application/CollectablesService.kt
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/port/out/ProductRepository.kt
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/persistence/repository/ProductRepositoryAdapter.kt
autonomous: true
requirements: [DETAIL-01, DETAIL-02, DETAIL-04]

must_haves:
  truths:
    - User can retrieve detailed information for any Pokémon product by cmId
    - Product details include language-specific pricing
    - Product details persist with all attributes
  artifacts:
    - path: "src/main/kotlin/io/github/havonte1/tcgwatcher/backend/application/CollectablesService.kt"
      provides: "Enhanced service handling language-specific details"
    - path: "src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/persistence/repository/ProductRepositoryAdapter.kt"
      provides: "Persistence layer for enhanced product data"
  key_links:
    - from: "CollectablesService.kt"
      to: "ProductRepositoryAdapter.kt"
      via: "ProductRepository.save() with language pricing"
      pattern: "productRepository.save.*Product"
---

<objective>
Update application layer to handle enhanced product details with language-specific pricing and product attributes. Ensure proper persistence of all data.

Purpose: Wire enhanced domain models through service and persistence layers
Output: Updated service and repository implementations
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/main/kotlin/io/github/havonte1/tcgwatcher/backend/application/CollectablesService.kt
@src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/port/out/ProductRepository.kt
@src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/model/Product.kt

# Service needs to handle enhanced Product with languagePricing and productAttributes
# Repository needs to persist these new fields
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ProductRepository interface</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/port/out/ProductRepository.kt</files>
  <action>
Add findByCmIdWithDetails method that fetches full product with language pricing if needed.

Add any additional repository methods needed for enhanced product queries.
  </action>
  <verify>
<automated>./gradlew compileKotlin 2>&1 | tail -20</automated>
  </verify>
  <done>ProductRepository interface updated for enhanced queries</done>
</task>

<task type="auto">
  <name>Task 2: Update CollectablesService for enhanced details</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/application/CollectablesService.kt</files>
  <action>
Update fetchProductDetails method:
- Ensure scraper is called for fresh data when product exists
- Pass through language-specific pricing from scraper result
- Ensure all productAttributes are captured
- Handle hasChanges() comparison including new language pricing fields

The existing logic already handles caching and change detection - verify and update hasChanges() method to compare languagePricing and productAttributes.
  </action>
  <verify>
<automated>./gradlew test --tests "*CollectablesServiceTest" -x 2>&1 | tail -30</automated>
  </verify>
  <done>CollectablesService updated to handle enhanced product details</done>
</task>

<task type="auto">
  <name>Task 3: Update ProductRepositoryAdapter for persistence</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/persistence/repository/ProductRepositoryAdapter.kt</files>
  <action>
Update save() method to handle:
- languagePricing list (needs entity conversion)
- productAttributes list (needs entity conversion)
- releaseDate, cardNumber fields

Review existing JPA entities (ProductEntity, SellOfferEntity) and add any missing columns for new fields.

If new entities needed (LanguagePricingEntity, ProductAttributeEntity), create them and add Liquibase migration.
  </action>
  <verify>
<automated>./gradlew compileKotlin 2>&1 | tail -20</automated>
  </verify>
  <done>ProductRepositoryAdapter persists enhanced product data</done>
</task>

</tasks>

<verification>
- [ ] ProductRepository interface supports enhanced product queries
- [ ] CollectablesService handles language-specific pricing in change detection
- [ ] Persistence layer stores languagePricing and productAttributes
- [ ] Build compiles successfully
</verification>

<success_criteria>
- Product details with language-specific pricing persist correctly
- Product details with all attributes persist correctly
- hasChanges() comparison includes new fields
</success_criteria>

<output>
After completion, create `.planning/phases/03-details-completion/03-03-SUMMARY.md`
</output>

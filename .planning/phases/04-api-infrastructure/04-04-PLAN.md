---
phase: 04-api-infrastructure
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/rest/CollectablesAdapter.kt
  - contract/openapi.yaml
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03

must_haves:
  truths:
    - "All endpoints return data directly without envelope wrappers"
    - "Pagination metadata is returned in response headers (X-Total-Count, X-Page, X-Limit)"
    - "API is publicly accessible without any authentication requirement"
  artifacts:
    - path: "src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/rest/CollectablesAdapter.kt"
      provides: "REST endpoints returning direct data"
      contains: "ResponseEntity"
    - path: "contract/openapi.yaml"
      provides: "API contract documentation"
      contains: "X-Total-Count"
  key_links:
    - from: "CollectablesAdapter.kt"
      to: "HTTP response"
      via: "ResponseEntity"
      pattern: "ResponseEntity.ok.*body"
---

<objective>
Verify that current implementation already meets criteria 1-3: direct response format, pagination headers, and public access.

Purpose: Confirm existing implementation matches requirements and document the current state.
Output: Verification report showing current implementation meets criteria.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/rest/CollectablesAdapter.kt
@contract/openapi.yaml

# Current implementation already has:
# - Direct response format (line 128: .body(dtoList) - returns List<ProductDTO> directly)
# - Pagination headers (lines 124-127: X-Total-Count, X-Page, X-Limit, X-Total-Pages)
# - No authentication annotations (no @PreAuthorize, no security config)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify direct response format implementation</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/rest/CollectablesAdapter.kt</files>
  <action>
Verify the current implementation returns data directly without envelope wrappers:

1. Check listCollectables method (line 56-129):
   - Returns `ResponseEntity<List<ProductDTO>>` - direct list, not wrapped
   - Body is `dtoList` which is `List<ProductDTO>` - direct array response
   - No envelope like {data: [...], meta: {...}}

2. Check getProductDetails method (line 132-167):
   - Returns `ResponseEntity<ProductDetailsDTO>` - single object, not wrapped
   - Body is `dto` which is `ProductDetailsDTO` - direct object response
   - No envelope wrapper

Verify this is correct by checking the API contract matches:
- openapi.yaml line 78: type: array with items ProductDTO (direct array response)
- openapi.yaml line 145: schema ProductDetailsDTO (direct object response)
  </action>
  <verify>
<automated>grep -n "ResponseEntity<List\|ResponseEntity<ProductDetails\|\.body(" src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/rest/CollectablesAdapter.kt | head -10</automated>
  </verify>
  <done>Direct response format verified - no envelope wrappers</done>
</task>

<task type="auto">
  <name>Task 2: Verify pagination headers implementation</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/rest/CollectablesAdapter.kt</files>
  <action>
Verify pagination metadata is returned in response headers:

1. Check listCollectables method returns these headers:
   - Line 124: X-Total-Count: searchResponse.totalCount
   - Line 125: X-Page: searchResponse.page
   - Line 126: X-Limit: searchResponse.limit
   - Line 127: X-Total-Pages: calculated from totalCount and limit

2. Verify openapi.yaml documents these headers:
   - Lines 67-74: X-Total-Count, X-Page, X-Limit, X-Total-Pages headers defined
   - Lines 62-74: Response 200 includes these headers

3. Confirm header names match API contract exactly.
  </action>
  <verify>
<automated>grep -n "X-Total-Count\|X-Page\|X-Limit\|X-Total-Pages" src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/rest/CollectablesAdapter.kt contract/openapi.yaml | head -20</automated>
  </verify>
  <done>Pagination headers verified - all four headers present</done>
</task>

<task type="auto">
  <name>Task 3: Verify public access (no authentication)</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/rest/CollectablesAdapter.kt</files>
  <action>
Verify API is publicly accessible without authentication:

1. Check CollectablesAdapter class:
   - No @PreAuthorize annotations
   - No @Secured annotations
   - No role-based access checks
   - No security-related method interceptors

2. Check for Spring Security configuration:
   - No SecurityConfig or WebSecurityConfigurerAdapter found
   - No @EnableWebSecurity annotations in codebase

3. Verify no security starter dependency:
   - spring-boot-starter-security not present in build.gradle.kts

4. Confirm rate limiter is the only access control mechanism (per design decision).
  </action>
  <verify>
<automated>grep -r "@PreAuthorize\|SecurityConfig\|@EnableWebSecurity\|spring-boot-starter-security" src/main/kotlin build.gradle.kts 2>/dev/null | head -5 || echo "No security found - API is public"</automated>
  </verify>
  <done>Public access verified - no authentication required</done>
</task>

</tasks>

<verification>
- [ ] Direct response format confirmed (List<ProductDTO>, ProductDetailsDTO)
- [ ] Pagination headers confirmed (X-Total-Count, X-Page, X-Limit, X-Total-Pages)
- [ ] No authentication required - no Spring Security, no @PreAuthorize
- [ ] Build succeeds with `./gradlew build -x test`
</verification>

<success_criteria>
All three criteria verified as already implemented in current codebase. No changes needed.
</success_criteria>

<output>
After completion, create `.planning/phases/04-api-infrastructure/{phase}-04-SUMMARY.md`
</output>

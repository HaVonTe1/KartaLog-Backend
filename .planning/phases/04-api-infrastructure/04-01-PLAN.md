---
phase: 04-api-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/application.yml
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/config/Resilience4jConfig.kt
autonomous: true
requirements:
  - API-04

must_haves:
  truths:
    - "Rate limiting throttles requests to protect CardMarket scraper"
    - "Exceeding rate limit returns 429 status"
  artifacts:
    - path: "src/main/resources/application.yml"
      provides: "Rate limiter configuration"
      contains: "ratelimiter"
    - path: "src/main/kotlin/io/github/havonte1/tcgwatcher/backend/config/Resilience4jConfig.kt"
      provides: "Rate limiter bean configuration"
      contains: "RateLimiterConfig"
  key_links:
    - from: "CollectablesAdapter.kt"
      to: "resilience4j rate limiter"
      via: "@RateLimiter annotation"
      pattern: "apiRateLimiter"
---

<objective>
Configure server-side rate limiting to protect CardMarket scraper from overuse.

Purpose: Prevent API consumers from overwhelming the CardMarket scraper, which could lead to IP bans or service degradation.
Output: Configured rate limiter with proper limits and fallback behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/rest/CollectablesAdapter.kt
@src/main/resources/application.yml

# Current rate limiter annotations already in place
From CollectablesAdapter.kt:
- Line 21: import io.github.resilience4j.ratelimiter.annotation.RateLimiter
- Line 36: @RateLimiter(name = "apiRateLimiter")
- Line 131: @RateLimiter(name = "apiRateLimiter")

# Current application.yml has no rate limiter configuration
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rate limiter configuration to application.yml</name>
  <files>src/main/resources/application.yml</files>
  <action>
Add Resilience4j rate limiter configuration under `resilience4j.ratelimiter.instances`:

```yaml
resilience4j:
  ratelimiter:
    instances:
      apiRateLimiter:
        limit-for-period: 10          # 10 requests per period
        limit-refresh-period: 1s      # Refresh every 1 second
        timeout-duration: 0s          # Don't wait, fail immediately when limit reached
        register-health-indicator: true
        event-consumer-buffer: 100
```

This configures the rate limiter to allow 10 requests per second, with immediate failure when limit is reached (no waiting).
  </action>
  <verify>
<automated>grep -A 10 "ratelimiter:" src/main/resources/application.yml</automated>
  </verify>
  <done>Rate limiter configuration present in application.yml with limit-for-period=10, limit-refresh-period=1s</done>
</task>

<task type="auto">
  <name>Task 2: Update Resilience4jConfig with rate limiter config customizer</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/config/Resilience4jConfig.kt</files>
  <action>
Update Resilience4jConfig to include rate limiter configuration bean. Add a Bean that configures the rate limiter:

```kotlin
@Bean
fun apiRateLimiterConfigCustomizer(): RateLimiterConfigCustomizer =
    RateLimiterConfigCustomizer.of("apiRateLimiter") { builder ->
        builder.limitRefreshPeriod(Duration.ofSeconds(1))
            .limitForPeriod(10)
            .timeoutDuration(Duration.ZERO)
    }
```

Import required classes:
- io.github.resilience4j.common.ratelimiter.configuration.RateLimiterConfigCustomizer
- java.time.Duration
  </action>
  <verify>
<automated>grep -n "RateLimiterConfigCustomizer\|apiRateLimiter" src/main/kotlin/io/github/havonte1/tcgwatcher/backend/config/Resilience4jConfig.kt</automated>
  </verify>
  <done>Resilience4jConfig includes rate limiter customizer bean</done>
</task>

</tasks>

<verification>
- [ ] Rate limiter configuration exists in application.yml
- [ ] Rate limiter customizer exists in Resilience4jConfig
- [ ] Build succeeds with `./gradlew build -x test`
</verification>

<success_criteria>
Rate limiting is configured and throttles requests when limit is exceeded. API returns 429 Too Many Requests when rate limit is hit.
</success_criteria>

<output>
After completion, create `.planning/phases/04-api-infrastructure/{phase}-01-SUMMARY.md`
</output>

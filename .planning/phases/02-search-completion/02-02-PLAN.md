---
phase: 02-search-completion
plan: 02
type: execute
wave: 1
depends_on: [01-05]
files_modified:
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/port/out/CardMarketScraperPort.kt
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketScraperAdapter.kt
autonomous: true
requirements: [SEARCH-01, SEARCH-02, SEARCH-03, SEARCH-04, SEARCH-05, SEARCH-06, SEARCH-07, SEARCH-08, SEARCH-09, SEARCH-10, SEARCH-11, SEARCH-12, SEARCH-13]

must_haves:
  truths:
    - User can search Pokémon products by free-text query and get relevant results
    - User can filter results by language, price range, condition, availability, and all CardMarket-supported filter parameters
    - User can sort results by price, name, release date, relevance, and all CardMarket-supported sort options (each asc/desc)
    - Search results are paginated with page/limit parameters and response includes total count and pagination metadata
  artifacts:
    - path: src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/port/out/CardMarketScraperPort.kt
      provides: Updated search method accepting SearchFilters, SortOptions, PaginationParams
    - path: src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketScraperAdapter.kt
      provides: Implementation passes all params to web fetcher and returns paginated result with total count
  key_links:
    - from: CardMarketScraperPort.search()
      to: CardMarketWebFetcher.fetch()
      via: Pass SearchFilters, SortOptions, PaginationParams to build proper URL
    - from: CardMarketScraperAdapter
      to: SearchResult (domain model)
      via: Return paginated result with total count (not just List<Product>)
---

<objective>
Update CardMarketScraperPort interface to accept search filters, sort options, and pagination parameters.

Purpose: Extend the port contract to support comprehensive search with all filter/sort/pagination options
Output: Updated CardMarketScraperPort.search() method signature and implementation
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 2 requirements: SEARCH-01 to SEARCH-13)
@.planning/phases/02-search-completion/02-01-PLAN.md (Plan 01 created SearchFilters, SortOptions, PaginationParams, CardMarketSearchParams)
@src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/port/out/CardMarketScraperPort.kt
@src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/model/SearchResult.kt
@src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketSearchParams.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CardMarketScraperPort interface</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/port/out/CardMarketScraperPort.kt</files>
  <action>
    Update search method signature to accept new parameters:
    suspend fun search(
      searchString: String,
      locale: Locale,
      genre: Genre,
      filters: SearchFilters?,
      sort: SortOption?,
      pagination: PaginationParams?,
    ): SearchResult

    Add import for SearchFilters, SortOptions, PaginationParams, SortOption, SearchResult
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | head -30</automated>
  </verify>
  <done>CardMarketScraperPort.search() accepts SearchFilters, SortOption, PaginationParams and returns SearchResult</done>
</task>

<task type="auto">
  <name>Task 2: Update CardMarketScraperAdapter implementation</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketScraperAdapter.kt</files>
  <action>
    Update CardMarketScraperAdapter.search() to:
    - Accept SearchFilters?, SortOption?, PaginationParams? in method signature
    - Build URL parameters using CardMarketSearchParams helper (filtersToParams, sortToParams, paginationToParams)
    - Pass parameters to webFetcher.fetch() - need to extend CardMarketWebFetcherPort interface
    - Parse total count from gallery page (look for pagination info element)
    - Return SearchResult with products list and totalCount
    If pagination is null, use defaults: PaginationParams(page=1, limit=30)
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | head -30</automated>
  </verify>
  <done>CardMarketScraperAdapter.search() accepts filters, sort, pagination and returns SearchResult with totalCount</done>
</task>

<task type="auto">
  <name>Task 3: Extend CardMarketWebFetcherPort for params</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketWebFetcherPort.kt</files>
  <action>
    Extend CardMarketWebFetcherPort.fetch() signature to accept:
    - searchString: String
    - locale: Locale
    - genre: Genre
    - filters: SearchFilters? (nullable, null = no filters)
    - sort: SortOption? (nullable, null = relevance sort)
    - pagination: PaginationParams? (nullable, null = page 1, limit 30)

    Update CardMarketWebFetcher implementation to use CardMarketSearchParams to build URL with all parameters
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | head -30</automated>
  </verify>
  <done>CardMarketWebFetcherPort.fetch() accepts filter/sort/pagination params and builds proper CardMarket URL</done>
</task>

</tasks>

<verification>
[ ] All files compile without errors
[ ] CardMarketScraperPort.search() accepts filters, sort, pagination params
[ ] CardMarketScraperAdapter.search() returns SearchResult with totalCount
[ ] CardMarketWebFetcher builds URL with all filter/sort/pagination parameters
</verification>

<success_criteria>
Scraper port accepts all search parameters and returns paginated results with total count
</success_criteria>

<output>
After completion, create `.planning/phases/02-search-completion/02-02-SUMMARY.md`
</output>
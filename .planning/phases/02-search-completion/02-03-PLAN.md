---
phase: 02-search-completion
plan: 03
type: execute
wave: 2
depends_on: [02-02]
files_modified:
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/application/SearchUseCase.kt
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/application/CollectablesService.kt
autonomous: true
requirements: [SEARCH-01, SEARCH-02, SEARCH-03, SEARCH-04, SEARCH-05, SEARCH-06, SEARCH-07, SEARCH-08, SEARCH-09, SEARCH-10, SEARCH-11, SEARCH-12, SEARCH-13]

must_haves:
  truths:
    - User can search Pokémon products by free-text query and get relevant results
    - User can filter results by language, price range, condition, availability, and all CardMarket-supported filter parameters
    - User can sort results by price, name, release date, relevance, and all CardMarket-supported sort options (each asc/desc)
    - Search results are paginated with page/limit parameters and response includes total count and pagination metadata
  artifacts:
    - path: src/main/kotlin/io/github/havonte1/tcgwatcher/backend/application/SearchUseCase.kt
      provides: Updated search() method accepting all search params, returning SearchResponse with pagination
    - path: src/main/kotlin/io/github/havonte1/tcgwatcher/backend/application/CollectablesService.kt
      provides: Implementation delegates to CardMarketScraperPort with all parameters
  key_links:
    - from: SearchUseCase.search()
      to: CardMarketScraperPort.search()
      via: Pass SearchFilters, SortOption, PaginationParams
    - from: CollectablesService
      to: CollectablesMapper
      via: Map SearchResponse to List<ProductDTO> for REST response
---

<objective>
Update application layer (SearchUseCase and CollectablesService) to support comprehensive search with filters, sorting, and pagination.

Purpose: Extend use case layer to handle all search parameters and return paginated results
Output: Updated SearchUseCase interface and CollectablesService implementation
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 2 requirements: SEARCH-01 to SEARCH-13)
@.planning/phases/02-search-completion/02-02-PLAN.md (Plan 02 updated CardMarketScraperPort)
@src/main/kotlin/io/github/havonte1/tcgwatcher/backend/application/SearchUseCase.kt (existing interface)
@src/main/kotlin/io/github/havonte1/tcgwatcher/backend/application/CollectablesService.kt (existing service)
@src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/model/SearchResult.kt (existing model)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define SearchResponse data class</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/application/SearchUseCase.kt</files>
  <action>
    In SearchUseCase.kt, add SearchResponse data class:
    - products: List<Product>
    - totalCount: Int
    - page: Int
    - limit: Int

    Update SearchUseCase.search() signature to accept:
    - searchString: String
    - locale: Locale (default to ENGLISH)
    - genre: Genre (default to POKEMON)
    - filters: SearchFilters? (nullable)
    - sort: SortOption? (nullable)
    - pagination: PaginationParams? (nullable)
    Returns: SearchResponse
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | head -30</automated>
  </verify>
  <done>SearchUseCase.search() accepts all search params and returns SearchResponse with pagination</done>
</task>

<task type="auto">
  <name>Task 2: Update CollectablesService.search()</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/application/CollectablesService.kt</files>
  <action>
    Update CollectablesService.search() method to:
    - Accept query, locale, genre, filters, sort, pagination parameters (matching SearchUseCase)
    - Call cardMarketScraper.search() with all parameters
    - Map result to SearchResponse with products, totalCount, page, limit
    - Preserve existing caching via @Cacheable("listCache") - use query + filters + sort + pagination as cache key
    Keep backward compatibility: if called without new params, use defaults (no filters, relevance sort, page 1, limit 30)
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | head -30</automated>
  </verify>
  <done>CollectablesService.search() accepts filters, sort, pagination and returns SearchResponse</done>
</task>

<task type="auto">
  <name>Task 3: Add service method for filters/sort/pagination</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/application/CollectablesService.kt</files>
  <action>
    Add new method for search with full parameters:
    suspend fun searchWithFilters(
      query: String,
      locale: Locale = Locale.ENGLISH,
      genre: Genre = Genre.POKEMON,
      filters: SearchFilters? = null,
      sort: SortOption? = null,
      pagination: PaginationParams? = null,
    ): SearchResponse

    This provides a clean API while maintaining backward compatibility with existing search()
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | head -30</automated>
  </verify>
  <done>CollectablesService has dedicated searchWithFilters method with full parameter support</done>
</task>

</tasks>

<verification>
[ ] All files compile without errors
[ ] SearchUseCase.search() accepts filters, sort, pagination
[ ] CollectablesService.search() passes params to scraper and returns SearchResponse
[ ] Caching works with full parameter key
</verification>

<success_criteria>
Application layer supports comprehensive search with all filters, sorting, and pagination
</success_criteria>

<output>
After completion, create `.planning/phases/02-search-completion/02-03-SUMMARY.md`
</output>
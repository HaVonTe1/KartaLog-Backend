---
phase: 02-search-completion
plan: 01
type: execute
wave: 1
depends_on: [01-05]
files_modified:
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/model/SearchFilters.kt
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/model/SortOptions.kt
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/model/PaginationParams.kt
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketSearchParams.kt
autonomous: true
requirements: [SEARCH-01, SEARCH-02, SEARCH-03, SEARCH-04, SEARCH-05, SEARCH-06, SEARCH-07, SEARCH-08, SEARCH-09, SEARCH-10, SEARCH-11, SEARCH-12, SEARCH-13]

must_haves:
  truths:
    - User can search Pokémon products by free-text query and get relevant results
    - User can filter results by language, price range, condition, availability, and all CardMarket-supported filter parameters
    - User can sort results by price, name, release date, relevance, and all CardMarket-supported sort options (each asc/desc)
    - Search results are paginated with page/limit parameters and response includes total count and pagination metadata
  artifacts:
    - path: src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/model/SearchFilters.kt
      provides: Domain models for search filter parameters
    - path: src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/model/SortOptions.kt
      provides: Sort options enum with all CardMarket-supported sort fields and directions
    - path: src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/model/PaginationParams.kt
      provides: Pagination parameters (page, limit) with validation
    - path: src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketSearchParams.kt
      provides: CardMarket-specific URL parameter builder for filters, sorting, and pagination
  key_links:
    - from: SearchFilters
      to: CardMarketSearchParams
      via: toUrlParams() method
    - from: SortOptions
      to: CardMarketSearchParams
      via: toUrlParams() method
---

<objective>
Define domain models for search parameters, sorting, and pagination that represent all CardMarket filter/sort/pagination capabilities.

Purpose: Establish type-safe contracts for search functionality before implementation
Output: SearchFilters, SortOptions, PaginationParams data classes and CardMarketSearchParams helper
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 2 requirements: SEARCH-01 to SEARCH-13)
@.planning/STATE.md (Key decisions: public API, offset-based pagination, direct response, pagination in headers)
@src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/model/Locale.kt (existing enum with 8 CardMarket languages)
@src/main/kotlin/io/github/havonte1/tcgwatcher/backend/config/GenreConfig.kt (Genre enum POKEMON, YUGIOH, MTG)

From Phase 1 SUMMARYs:
- Locale enum has 8 languages: GERMAN, ENGLISH, FRENCH, ITALIAN, SPANISH, PORTUGUESE, DUTCH, POLISH
- Genre enum: POKEMON, YUGIOH, MTG
- TranslationMap already handles labels per locale
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SearchFilters domain model</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/model/SearchFilters.kt</files>
  <action>
    Create SearchFilters data class with all CardMarket-supported filter parameters:
    - language: Locale? (filter by card language)
    - minPrice: BigDecimal? (minimum price filter)
    - maxPrice: BigDecimal? (maximum price filter)
    - condition: Condition? (NM, LP, MP, HP, DM)
    - isAvailable: Boolean? (filter by availability)
    - isFoil: Boolean? (foil filter)
    - isSigned: Boolean? (signed filter)
    - isAltered: Boolean? (altered filter)
    - isPlayset: Boolean? (playset filter)
    Include sealed class Condition with values: NEAR_MINT, LIGHTLY_PLAYED, MODERATELY_PLAYED, HEAVILY_PLAYED, DAMAGED
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | head -30</automated>
  </verify>
  <done>SearchFilters data class exists with all CardMarket filter parameters as nullable fields</done>
</task>

<task type="auto">
  <name>Task 2: Create SortOptions domain model</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/model/SortOptions.kt</files>
  <action>
    Create SortOptions sealed class with all CardMarket-supported sort options:
    - SortField enum: PRICE, NAME, RELEVANCE, RELEASE_DATE, AVG_PRICE, AMOUNT
    - SortDirection enum: ASC, DESC
    - data class SortOption(field: SortField, direction: SortDirection)
    Include companion object with defaultSort() returning SortOption(SortField.RELEVANCE, Direction.ASC)
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | head -30</automated>
  </verify>
  <done>SortOptions sealed class exists with all sort fields and directions</done>
</task>

<task type="auto">
  <name>Task 3: Create PaginationParams model</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/model/PaginationParams.kt</files>
  <action>
    Create PaginationParams data class:
    - page: Int (1-based, default 1)
    - limit: Int (default 20, max 100)
    Include validation in init block: page >= 1, limit in 1..100
    Include computed properties: offset (page-1)*limit, hasMore (total > offset+limit)
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | head -30</automated>
  </verify>
  <done>PaginationParams data class exists with page/limit validation and computed offset</done>
</task>

<task type="auto">
  <name>Task 4: Create CardMarketSearchParams helper</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketSearchParams.kt</files>
  <action>
    Create CardMarketSearchParams object with methods to build URL parameters:
    - filtersToParams(SearchFilters): Map<String, String> - converts filters to CardMarket URL params
    - sortToParams(SortOption): Map<String, String> - converts sort to URL params
    - paginationToParams(PaginationParams): Map<String, String> - converts to start/limit params
    Reference actual CardMarket URL patterns (e.g., idProductLanguage, priceMin, priceMax, condition, isAvailable, sort, dir, start, limit)
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | head -30</automated>
  </verify>
  <done>CardMarketSearchParams object exists with URL parameter builder methods</done>
</task>

</tasks>

<verification>
[ ] All 4 files created and compile without errors
[ ] SearchFilters includes all CardMarket filter parameters
[ ] SortOptions includes all sort fields and directions
[ ] PaginationParams validates page >= 1, limit in 1..100
[ ] CardMarketSearchParams converts all domain models to URL parameters
</verification>

<success_criteria>
Type-safe search parameters exist for filters, sorting, and pagination
</success_criteria>

<output>
After completion, create `.planning/phases/02-search-completion/02-01-SUMMARY.md`
</output>
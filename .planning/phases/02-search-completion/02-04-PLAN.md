---
phase: 02-search-completion
plan: 04
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - contract/openapi.yaml
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/rest/CollectablesAdapter.kt
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/rest/CollectablesMapper.kt
autonomous: true
requirements: [SEARCH-01, SEARCH-02, SEARCH-03, SEARCH-04, SEARCH-05, SEARCH-06, SEARCH-07, SEARCH-08, SEARCH-09, SEARCH-10, SEARCH-11, SEARCH-12, SEARCH-13]

must_haves:
  truths:
    - User can search Pokémon products by free-text query and get relevant results
    - User can filter results by language, price range, condition, availability, and all CardMarket-supported filter parameters
    - User can sort results by price, name, release date, relevance, and all CardMarket-supported sort options (each asc/desc)
    - Search results are paginated with page/limit parameters and response includes total count and pagination metadata
  artifacts:
    - path: contract/openapi.yaml
      provides: Updated /collectables/ endpoint with filter, sort, pagination query parameters
    - path: src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/rest/CollectablesAdapter.kt
      provides: Implementation extracts query params and calls service with all parameters
    - path: src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/rest/CollectablesMapper.kt
      provides: Map SearchResponse to paginated response with pagination headers
  key_links:
    - from: CollectablesAdapter
      to: CollectablesService
      via: Pass query params as SearchFilters, SortOption, PaginationParams
    - from: CollectablesAdapter response
      to: HTTP headers
      via: Add X-Total-Count, X-Page, X-Limit, X-Total-Pages headers per API-02 decision
---

<objective>
Update REST API endpoint to accept filter, sort, and pagination query parameters, and return pagination metadata in response headers.

Purpose: Expose search functionality via REST API with all CardMarket filter/sort/pagination options
Output: Updated OpenAPI spec and REST adapter implementation
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 2 requirements: SEARCH-01 to SEARCH-13)
@.planning/STATE.md (Key decision: pagination in headers)
@.planning/phases/02-search-completion/02-03-PLAN.md (Plan 03 updated CollectablesService)
@src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/rest/CollectablesAdapter.kt
@contract/openapi.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update OpenAPI spec with search params</name>
  <files>contract/openapi.yaml</files>
  <action>
    Add query parameters to GET /collectables/:
    - Filter params: language, minPrice, maxPrice, condition, isAvailable, isFoil, isSigned, isAltered, isPlayset
    - Sort params: sortField (price, name, relevance, releaseDate, avgPrice, amount), sortDir (asc, desc)
    - Pagination params: page (default 1), limit (default 20, max 100)

    Add response headers for pagination:
    - X-Total-Count: Total number of results
    - X-Page: Current page
    - X-Limit: Items per page
    - X-Total-Pages: Total pages available

    Update ProductDTO schema if needed for any new fields
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | head -30</automated>
  </verify>
  <done>OpenAPI spec has all filter, sort, pagination params and pagination response headers</done>
</task>

<task type="auto">
  <name>Task 2: Update CollectablesAdapter implementation</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/rest/CollectablesAdapter.kt</files>
  <action>
    Update listCollectables() method to:
    - Extract filter params from query params (language, minPrice, maxPrice, condition, isAvailable, isFoil, isSigned, isAltered, isPlayset)
    - Extract sort params (sortField, sortDir) and build SortOption
    - Extract pagination params (page, limit) and build PaginationParams
    - Build SearchFilters from extracted params
    - Call collectablesService.searchWithFilters() with all params
    - Add pagination headers to response: X-Total-Count, X-Page, X-Limit, X-Total-Pages
    - Return List<ProductDTO> from SearchResponse.products

    Handle edge cases:
    - Invalid page/limit values → return 400 Bad Request
    - Invalid condition/sort values → return 400 Bad Request
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | head -30</automated>
  </verify>
  <done>CollectablesAdapter extracts query params and returns paginated response with headers</done>
</task>

<task type="auto">
<parameter name>Task 3: Update CollectablesMapper for SearchResponse</name>
  <files>src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/inbound/rest/CollectablesMapper.kt</files>
  <action>
    Add methods to CollectablesMapper:
    - toSearchResponseDto(SearchResponse): map products to List<ProductDTO>
    - toPaginationHeaders(SearchResponse): Map<String, String> with X-Total-Count, X-Page, X-Limit, X-Total-Pages
    
    Helper methods to extract and format pagination metadata
  </action>
  <verify>
    <automated>./gradlew compileKotlin 2>&1 | head -30</automated>
  </verify>
  <done>CollectablesMapper can map SearchResponse to DTOs and pagination headers</done>
</task>

</tasks>

<verification>
[ ] All files compile without errors
[ ] OpenAPI spec includes all filter/sort/pagination parameters
[ ] OpenAPI spec includes pagination response headers
[ ] REST adapter extracts and passes all params to service
[ ] Response includes pagination headers (X-Total-Count, X-Page, X-Limit, X-Total-Pages)
</verification>

<success_criteria>
REST API supports all search filters, sort options, and pagination with proper response headers
</success_criteria>

<output>
After completion, create `.planning/phases/02-search-completion/02-04-SUMMARY.md`
</output>
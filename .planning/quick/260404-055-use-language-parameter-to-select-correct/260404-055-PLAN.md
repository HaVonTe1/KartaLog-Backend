---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketDetailsParser.kt
  - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketScraperAdapter.kt
autonomous: true
requirements:
  - quick-055
must_haves:
  truths:
    - DetailsParser uses TranslationMap labels for all label-based lookups (rarity, set info, price, price trend)
    - Correct language labels are selected based on the lang parameter
  artifacts:
    - path: src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketDetailsParser.kt
      provides: Product details parsing with language-aware label lookups
  key_links:
    - from: CardMarketScraperAdapter.fetchProductDetails
      to: CardMarketDetailsParser.parse
      via: passes lang parameter + TranslationMap
      pattern: detailsParser.parse.*lang.*translationMap
---

<objective>
Use the language parameter to select the correct TranslationMap in CardMarketDetailsParser for label-based lookups (rarity, release date, price, price trend).

Purpose: Replace hardcoded fallback logic with proper language-specific label selection from TranslationMap.
Output: Parser uses TranslationMap based on lang parameter.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/TranslationMap.kt
@src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketDetailsParser.kt
@src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketScraperAdapter.kt
@src/main/kotlin/io/github/havonte1/tcgwatcher/backend/domain/model/Locale.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DetailsParser to use TranslationMap for label lookups</name>
  <files>
    - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketDetailsParser.kt
    - src/main/kotlin/io/github/havonte1/tcgwatcher/backend/adapter/out/webscraper/cardmarket/CardMarketScraperAdapter.kt
  </files>
  <action>
    1. Modify `CardMarketDetailsParser.parse()` to accept `translationMap: TranslationMap` parameter.
    2. Create a helper function to select the correct Labels based on the `lang` parameter:
       ```kotlin
       private fun selectLabels(lang: String, map: TranslationMap): Labels {
           return when (lang) {
               "de" -> map.de
               "en" -> map.en
               "fr" -> map.fr
               "it" -> map.it
               "es" -> map.es
               "pt" -> map.pt
               "nl" -> map.nl
               "pl" -> map.pl
               else -> map.en  // fallback to English
           }
       }
       ```
    3. Update extraction methods to use the selected Labels instead of hardcoded fallback chains:
       - `extractRarity(dts, labels)` - use `labels.rarityLabel` instead of checking "Rarität", "Rarity", "Rareté"
       - `extractSetInfo(dts, labels)` - use `labels.releaseDateLabel` instead of "Erschienen in", "Released in", "Date de sortie"
       - `extractPrice(dts, labels)` - use `labels.priceLabel` instead of "ab", "from", "à partir de"
       - `extractPriceTrend(dts, labels)` - use `labels.priceTrendLabel` instead of "Preis-Trend", "Price trend", "Évolution des prix"
    4. Update `CardMarketScraperAdapter` to pass `DEFAULT_TRANSLATION_MAP` to the parsers in both `search()` and `fetchProductDetails()` methods.
    5. Remove the hardcoded fallback logic from each extraction method.
  </action>
  <verify>
    <automated>./gradlew test --tests "CardMarketDetailsParserTest" -x test 2>/dev/null || ./gradlew compileKotlin</automated>
  </verify>
  <done>
    DetailsParser uses TranslationMap labels for all label-based lookups, selecting the correct Labels based on lang parameter.
    Extraction methods no longer use hardcoded fallback chains.
  </done>
</task>

</tasks>

<verification>
- Parser uses TranslationMap for all label-based extractions
- lang parameter determines which language labels are used
- Hardcoded fallback chains removed from extraction methods
</verification>

<success_criteria>
- `extractRarity` uses `labels.rarityLabel` 
- `extractSetInfo` uses `labels.releaseDateLabel`
- `extractPrice` uses `labels.priceLabel`
- `extractPriceTrend` uses `labels.priceTrendLabel`
- Code compiles successfully
</success_criteria>

<output>
After completion, create `.planning/quick/260404-055-use-language-parameter-to-select-correct/260404-055-PLAN-SUMMARY.md`
</output>
